import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello World!")
            .font(.largeTitle)
            .foregroundColor(.white)
            .padding(.vertical)
            .background(.blue)
            .padding()
            .background(.mint)
            .padding()
            .background(.gray)
    }
}
